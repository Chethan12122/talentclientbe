export interface InstituteRequest {
  name: string;
  venue: string;
}

export interface InstituteResponse {
  institute_id: string;
  name: string;
  venue: string;
}

export interface VenueRequest {
  name: string;
}
